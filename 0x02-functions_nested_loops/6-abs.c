#include "main.h"
/**
 * _abs - use of the abs() function
 * @r: an integer from main file
 *
 * Description: display the valueof an integer
 * Return: Value of the absolute value r
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		/* multiply by -1 to return a negetive value */
		return (r * -1)
}
