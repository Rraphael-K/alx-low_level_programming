#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	/* declare the number placeholder */
	int num = 0;

	/* loop to print numbers in order */
	while (num < 10)
	{
		printf("%d", num);
		num++;
	} putchar('\n');
	return (0);
}

