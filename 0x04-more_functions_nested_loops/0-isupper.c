#include "main.h"
#include <stdio.h>

/**
 * _isupper - Determine if letter is Uppercase
 *
 * Description: Alphabet checking
 *
 * Return: i
 */

int _isupper(int c)
{
	if (c = 'A' || c <= 'Z')
		return (1);
	else
		return (0);		
}
