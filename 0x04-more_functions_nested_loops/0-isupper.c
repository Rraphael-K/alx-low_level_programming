#include "main.h"
#include <stdio.h>
/**
 * _isupper - See below
 * @c: main input
 * Description: Alphabet checking
 *
 * Return: 1 if c is uppercase or 0 if not
 */

int _isupper(int c)
{
	char upper = 'A';
	int isUpper = 0;

	for (; upper <= 'Z'; upper++)
	{
		if (c == upper)
		{
			if (c == upper)
			{
				isUpper = 1;
				/* break out of the loop */
				break;
			}
		}
		return (isupper);
}
